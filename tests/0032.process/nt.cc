#include <fast_io.h>

int main() {
	//::fast_io::native_process_io tjcli{ ::fast_io::in(), ::fast_io::out(), ::fast_io::err() };
	////try {
	//	::fast_io::native_process cli{ u8"C:\\Windows\\System32\\calc.exe", tjcli };
	////} catch (fast_io::error e) { perr(::fast_io::native_file{ u8"test.log",fast_io::open_mode::out }, e); }
}
