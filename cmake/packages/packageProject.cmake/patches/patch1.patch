diff --git a/CMakeLists.txt b/CMakeLists.txt
index 2c35c56..e1b52c7 100644
--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -12,7 +12,7 @@ function(packageProject)
   cmake_parse_arguments(
     PROJECT
     ""
-    "NAME;VERSION;INCLUDE_DIR;INCLUDE_DESTINATION;BINARY_DIR;COMPATIBILITY;EXPORT_HEADER;VERSION_HEADER;NAMESPACE;DISABLE_VERSION_SUFFIX;ARCH_INDEPENDENT;INCLUDE_HEADER_PATTERN;CPACK;"
+    "NAME;VERSION;INCLUDE_DIR;INCLUDE_DESTINATION;BINARY_DIR;INSTALL_CMAKEDIR;COMPATIBILITY;EXPORT_HEADER;VERSION_HEADER;NAMESPACE;DISABLE_VERSION_SUFFIX;ARCH_INDEPENDENT;INCLUDE_HEADER_PATTERN;CPACK;"
     "DEPENDENCIES"
     ${ARGN}
   )
@@ -126,7 +126,7 @@ function(packageProject)
   install(
     TARGETS ${PROJECT_NAME}
     EXPORT ${PROJECT_NAME}Targets
-    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}/${PROJECT_NAME}${PROJECT_VERSION_SUFFIX}
+    LIBRARY DESTINATION ${CMAKE_INSTALL_LIBDIR}
             COMPONENT "${PROJECT_NAME}_Runtime"
             NAMELINK_COMPONENT "${PROJECT_NAME}_Development"
     ARCHIVE DESTINATION ${CMAKE_INSTALL_LIBDIR}/${PROJECT_NAME}${PROJECT_VERSION_SUFFIX}
@@ -140,10 +140,15 @@ function(packageProject)
     DESTINATION "${PROJECT_INCLUDE_DESTINATION}"
   )

-  set("${PROJECT_NAME}_INSTALL_CMAKEDIR"
-      "${CMAKE_INSTALL_LIBDIR}/cmake/${PROJECT_NAME}${PROJECT_VERSION_SUFFIX}"
-      CACHE PATH "CMake package config location relative to the install prefix"
-  )
+  if(NOT DEFINED PROJECT_INSTALL_CMAKEDIR)
+    set("${PROJECT_NAME}_INSTALL_CMAKEDIR"
+        "${CMAKE_INSTALL_LIBDIR}/cmake/${PROJECT_NAME}${PROJECT_VERSION_SUFFIX}"
+        CACHE PATH "CMake package config location relative to the install prefix"
+    )
+  else()
+    set("${PROJECT_NAME}_INSTALL_CMAKEDIR" ${PROJECT_INSTALL_CMAKEDIR} CACHE
+    PATH "CMake package config location relative to the install prefix")
+  endif()

   mark_as_advanced("${PROJECT_NAME}_INSTALL_CMAKEDIR")
